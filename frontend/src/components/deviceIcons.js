import { IconBulb, IconDrop, IconMist, IconProjector } from "./Icons";

/** Ícone por dispositivo, usado no menu lateral e nos cards. */
export const DEVICE_ICON = {
  luz: IconBulb,
  projetor: IconProjector,
  irrigacao: IconDrop,
  umidificador: IconMist,
};
